package com.grenadier.core;

import net.minecraft.world.level.block.Block;

/** Compatibility shell for the legacy armsrace:rally_core registry entry. */
public class RallyCoreBlock extends Block {
    public RallyCoreBlock(Properties properties) {
        super(properties);
    }
}
